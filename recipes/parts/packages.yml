modules:
  - type: dnf
    repos:
      cleanup: true
      copr:
        enable:
          - cdayjr/yadm
    install:
      packages:
        # Terminal
        - alacritty
        - zsh
        - yadm
        # Devel
        - distrobox
        - podman
        - toolbox
  - type: brew
    nofile-limits: true
  - type: script
    scripts:
      - "install_vscode.sh"
