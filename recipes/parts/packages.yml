modules:
  - type: dnf
    repos:
      cleanup: true
      copr:
        enable:
          - cdayjr/yadm
    install:
      packages:
        # Terminal
        - zsh
        - yadm
        # Devel
        - distrobox
        - podman
        - toolbox
        # Networking
        - tailscale
  - type: brew
    nofile-limits: true
