modules:
  - type: dnf
    repos:
      cleanup: true
      copr:
        enable:
          - cdayjr/yadm
    install:
      packages:
        # Terminal
        - zsh
        - yadm
        - gum
        # Devel
        - distrobox
        - podman
        - toolbox
        # Networking
        - tailscale
